#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[])
{
  char* message = "From the screen to the ring, to the pеn, to the king";

  printf("Finding the length of '%s'\n", message);

  // int i;
  // while (message[i] != '\0') {
  //   i++;
  // }
  // printf("Length: %d\n", i);

  // /* Using the standard library function like a normal person */
  // printf("Length using strlen(): %lu\n", strlen(message));

  return 0;
}
